# Free Ollama API Gateway — runtime dependencies
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.3
pyyaml>=6.0
sqlalchemy>=2.0
aiosqlite>=0.20
# Драйвер Postgres обязателен: контейнерная топология §14.1 (docker-compose.yml)
# работает только на Postgres — при SQLite реплики шлюза не имеют общего
# состояния, а одноразовый шаг migrate не наведёт схему в общем каталоге.
asyncpg>=0.29
alembic>=1.13
prometheus-client>=0.20
cryptography>=42.0
PyJWT>=2.8
dnspython>=2.6
